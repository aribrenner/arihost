%h2
  create an account

= form_for @user do |form|
  = form.text_field :username
  = form.text_field :password, type: :password
  = submit_tag 'sign up'

%br

= link_to 'returning user?', new_session_path
